import { STYLES } from "../lib/styles";

const AddToCart = () => {
  return (
    <button id={"add-to-cart"} className={STYLES.ADD_TO_CART}>
      {"추가"}
    </button>
  );
};

export default AddToCart;
